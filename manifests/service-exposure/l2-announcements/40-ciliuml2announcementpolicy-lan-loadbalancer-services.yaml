apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: lan-loadbalancer-services
spec:
  externalIPs: false
  interfaces:
  - ^ens33$
  loadBalancerIPs: true
  nodeSelector:
    matchExpressions:
    - key: node-role.kubernetes.io/control-plane
      operator: DoesNotExist
  serviceSelector:
    matchLabels:
      lb-ipam: lan
