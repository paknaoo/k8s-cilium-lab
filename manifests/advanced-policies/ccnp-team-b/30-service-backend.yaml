apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend
    security-tier: protected
  name: backend
  namespace: ccnp-team-b
spec:
  internalTrafficPolicy: Cluster
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: backend
  sessionAffinity: None
  type: ClusterIP
